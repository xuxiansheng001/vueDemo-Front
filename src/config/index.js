/**
 * @description 项目配置文件
 */
export const AppConfig = {
    // 应用标题
    APP_TITLE: 'Vue3 Demo',
    
    // API 基础路径 - 使用相对路径即可，由vite代理到后端
    API_BASE_URL: '/api',
  
    // 请求超时时间 (毫秒)
    REQUEST_TIMEOUT: 10000,
  };